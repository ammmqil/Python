def Search_prost_del(num):
    for i in range(2, int(num**0.5)+1):
        if num%i == 0:
            return False
    return True

def Max_prost_divisor(num):
    list_max=[]
    for i in range(2, int(num**0.5)+1):
        if num%i == 0 and Search_prost_del(num):
            list_max.append(i)
            if Search_prost_del(num//i):
                list_max.append(num//i)
            return(max(list_max))

print(Max_prost_divisor(121))
