def Rotation_nums():
    print('Введите числа')
    numbers = list(map(int, input().split()))

    vars = all((numbers[i] > 0 and numbers[i + 1] < 0) or (numbers[i] < 0 and numbers[i + 1] > 0) for i in range(len(numbers) - 1))
    print("Чередуются" if vars else "Не чередуются")

Rotation_nums()
